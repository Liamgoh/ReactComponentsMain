//import React as we are using JSX to insert html elements in JavaScript (see below)
import React from "react";

//React convention to give React component names Pascal case. Components are written in JSX (React):
function Heading() {
  return <h1>My Favourite Foods</h1>;
}

export default Heading;
